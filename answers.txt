Q1: In your own words, explain what happens when you type a website address like google.com and press Enter.
When I type google.com into my browser and press Enter, the first step is a Domain Name System lookup, where DNS resolves the domain name into an IP address so the browser knows where to send the request.
The request is then sent to a load balancer, which distributes traffic across different web servers using algorithms such as round-robin to improve performance and prevent overload.
The web server receives the request and processes it. If the request requires only static content such as images, HTML, or files, the web server responds directly. However, if additional processing is needed, the request is forwarded to the application server.
The application server handles the business logic, runs the required code, and communicates with the database when information needs to be retrieved or stored. The response is then sent back through the web server and load balancer to the browser, where the webpage is displayed.

Q2: What is the difference between a client and a server? Give one real example of each.
Client is any device that can be used to send request and receive it back. Example, your phone using WhatsApp to send a message 
Whilst 
A server is a device that provides services data or information to the client when they request them. Example, the WhatsApp server that stores and delivers message to the receiver.

Q3:What is DNS and why do we need it?
 DNS (Domain Name System) converts website names like google.com into IP addresses that computers understand. We need it so users can easily access websites without remembering long IP addresses of every website we want to visit

Q4: A firewall and a load balancer are both part of how the web works. What does each one do? How are they different?
A firewall is a network security device that monitors and controls incoming and outgoing traffic to protect networks from unauthorized access and cyber threats.
Whilst
A load balancer helps distributes traffic from incoming requests across multiple servers to prevent overload and improve performance. The difference is that a firewall focuses on security, while a load balancer focuses on managing traffic and keeping services running smoothly.

Q5: Explain the difference between a web server and an application server. Which one does a backend developer work with more, and why?
A web server receives requests from the load balancer and handles them. If the request only needs static content, the web server sends the response directly. If additional functionality is needed, it forwards the request to the application server.

An application server accesses and executes available codebases, processes the request, communicates with the database when needed, and sends the response back to the web server.

A backend developer works more with the application server because they build and manage the application logic, APIs, and database operations that make the application function.
Q6: Why does the client never communicate directly with the database? What are the benefits of having an application server in between?
The client does not communicate directly with the database because it would expose the database to security risks and unauthorized access.
The application server acts as a bridge between the client and the database. It processes requests, retrieves or updates data, and sends the correct response back to the client.

Q7: Is a university department page (like ug.edu.gh/statistics) more likely to be a static or dynamic website? What about your MTN MoMo account page? Explain your answers.
A university department page like ug.edu.gh/statistics is more likely to be a static website because it mainly displays information that is the same for all visitors, such as courses, staff, and contact details.
An MTN Momo account page is a dynamic website because it shows personalized information like your account balance and transaction history, which is generated based on your account.

Q8: What is the main difference between frontend and backend development? If you were asked to build the login feature of a website, which side would handle checking your password against the database and why?
The frontend development is the part of a website that users see and interact with, while the backend development handles the server, application logic, and database.

The backend development checks the password against the database because it securely processes user data and protects important information.